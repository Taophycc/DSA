// Difficulty - EASY
/*
function runningSum(nums: number[]): number[] {
    let arr = []
    let sum = 0

    for(let i = 0; i < nums.length; i++) {
        sum += nums[i]
        arr.push(sum)
    }

    return arr
};
*/
